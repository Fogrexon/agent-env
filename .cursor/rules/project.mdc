---
description: agent-env 全体の目的・構成・Google ADK (TypeScript) 規約
alwaysApply: true
---

# agent-env

並列・自律エージェントのテンプレート / ハーネス。オーケストレーションは **Google ADK**、LLM は **`@agent-env/llm` の provider registry**。

## レイヤ

| 層 | 場所 | 役割 |
|----|------|------|
| Agents | `agents/*/agent.ts` | `rootAgent` |
| LLM | `packages/llm` | ファクトリ・registry・`resolveModel` |
| Harness | `packages/harness` | `runAgent` / `runFromSpec` / guarded tools |
| Shared | `packages/shared` | Zod（ModelRef・RunSpec・Event） |
| Docs | `docs/` | ARCHITECTURE + 研究レポート |

## 必須ルール

- 秘密情報・接続設定の取得方法は利用側（エージェント / アプリ）の責務。`create*Provider` / `create*Connector` に注入する。コネクタに `*FromEnv` や固定 env 名前提を置かない
- OpenAI 互換は `createOpenaiCompatibleProvider({ id, baseUrl, apiKey? })` を **id ごとに複数**登録してよい
- モデルは `resolveModel({ provider, model })`（登録済み provider id）
- 計測可能な実行は RunSpec + `runFromSpec`（成功は verifier、agent 自己申告ではない）
- ツール引数は Zod。権限境界は `createGuardedTool`（T0–T3）
- 並列分岐の `outputKey` は一意
- シークレットをコミットしない
- エージェント追加時は `packages/harness/src/registry.ts` も更新

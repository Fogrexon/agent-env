apiVersion: agent-env/v1
kind: AgentParams
agentId: investigator
title: Investigator
objectiveField: message
fields:
  - id: message
    type: text
    label: Investigation question
    required: true
    default: "What is agent-env's approach to verification (SUCCEEDED vs COMPLETED)?"

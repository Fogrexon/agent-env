apiVersion: agent-env/v1
kind: AgentParams
agentId: python-vision
title: Python Vision
objectiveField: message
fields:
  - id: message
    type: text
    label: Task
    required: true
    default: >-
      Triage image at /tmp/scene-person.jpg (path may not exist — mock detector
      still returns person boxes from the filename). Decide CLEAR / WATCH / ALERT.
